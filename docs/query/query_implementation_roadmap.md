# src/query 目录实施路线图

## 🗺️ 路线图概述

本实施路线图提供了 `src/query` 目录改进计划的详细执行指南，包括具体的实施步骤、检查清单、验收标准和风险控制措施。

## 📅 第一阶段：紧急修复（第1-2周）

### Day 1-2: 模块冗余清理

#### 🎯 目标
消除代码重复，简化架构，为后续改进奠定基础

#### 📋 检查清单

**Day 1: 清理解析器冗余**
- [ ] 确认新解析器功能完整性
  ```bash
  # 运行解析器测试
  cargo test parser::tests --lib
  ```
- [ ] 备份当前代码状态
  ```bash
  git checkout -b cleanup_redundancy
  git add .
  git commit -m "Backup before cleanup"
  ```
- [ ] 删除 `parser_old/` 目录
  ```bash
  rm -rf src/query/parser_old
  ```
- [ ] 更新所有相关导入
  ```bash
  # 搜索并更新所有引用parser_old的代码
  grep -r "parser_old" src/query/
  ```
- [ ] 验证编译通过
  ```bash
  cargo check --message-format=short 2>&1 | Select-String "error\[E" | Select-Object -First 10
  ```

**Day 2: 合并重复规划器**
- [ ] 比较两个go_planner.rs文件
  ```bash
  diff src/query/planner/go_planner.rs src/query/planner/ngql/go_planner.rs
  ```
- [ ] 确定保留版本（选择功能更完整的）
- [ ] 合并功能差异
- [ ] 更新规划器注册机制
- [ ] 删除重复文件
- [ ] 运行完整测试套件
  ```bash
  cargo test planner --lib
  ```

#### ✅ 验收标准
- [ ] 编译无警告
- [ ] 所有测试通过
- [ ] 代码行数减少15%以上
- [ ] 没有重复的文件或功能

#### 🚨 风险控制
- **风险**：删除重要功能
- **控制**：完整备份，分步提交，每步验证
- **回滚计划**：`git reset --hard <backup_commit>`

---

### Day 3-6: NGQL规划器实现

#### 🎯 目标
实现核心NGQL查询功能，使基本查询可用

#### 📋 检查清单

**Day 3: GO规划器基础实现**
- [ ] 分析当前GO规划器接口
  ```rust
  // 检查当前实现状态
  fn transform(&mut self, ast_ctx: &AstContext) -> Result<SubPlan, PlannerError> {
      // 当前返回错误，需要实现完整逻辑
  }
  ```
- [ ] 设计GO查询执行计划结构
- [ ] 实现基础GO查询规划
- [ ] 添加简单测试用例
- [ ] 验证基本GO查询可以解析

**Day 4: GO规划器高级功能**
- [ ] 实现多步扩展逻辑
- [ ] 添加边类型过滤
- [ ] 实现方向控制（in/out/both）
- [ ] 添加WHERE条件支持
- [ ] 实现YIELD子句处理
- [ ] 添加复杂测试用例

**Day 5: LOOKUP规划器实现**
- [ ] 分析LOOKUP查询需求
- [ ] 实现索引查找逻辑
- [ ] 添加属性过滤功能
- [ ] 实现YIELD子句支持
- [ ] 添加测试用例
- [ ] 验证LOOKUP查询功能

**Day 6: PATH规划器实现**
- [ ] 设计路径查询算法
- [ ] 实现基础路径遍历
- [ ] 添加最短路径查找
- [ ] 实现路径过滤条件
- [ ] 添加测试用例
- [ ] 验证PATH查询功能

#### ✅ 验收标准
- [ ] 基本GO查询可以执行并返回结果
- [ ] LOOKUP查询可以正确查找索引
- [ ] PATH查询可以找到路径
- [ ] 所有NGQL规划器测试通过
- [ ] 查询性能在可接受范围内

#### 🚨 风险控制
- **风险**：实现复杂度超出预期
- **控制**：分阶段实现，先支持基本功能
- **回滚计划**：保留占位符实现，逐步替换

---

### Day 7-9: 表达式处理完善

#### 🎯 目标
完成WHERE条件表达式和YIELD聚合逻辑

#### 📋 检查清单

**Day 7: WHERE表达式解析**
- [ ] 分析当前WHERE处理状态
  ```rust
  // 当前状态：TODO注释
  // TODO: 设置过滤条件表达式
  ```
- [ ] 设计表达式解析架构
- [ ] 实现基础表达式解析
- [ ] 添加比较操作符支持
- [ ] 实现逻辑操作符（AND/OR/NOT）
- [ ] 添加测试用例

**Day 8: 高级表达式功能**
- [ ] 实现函数调用支持
- [ ] 添加属性访问表达式
- [ ] 实现类型检查和转换
- [ ] 添加表达式优化
- [ ] 完善错误处理
- [ ] 添加复杂测试用例

**Day 9: YIELD聚合处理**
- [ ] 分析YIELD子句需求
- [ ] 实现聚合函数（COUNT/SUM/AVG等）
- [ ] 添加投影逻辑
- [ ] 实现别名处理
- [ ] 添加排序支持
- [ ] 完善测试用例

#### ✅ 验收标准
- [ ] WHERE条件可以正确过滤数据
- [ ] 复杂表达式可以正确解析和执行
- [ ] YIELD子句可以正确聚合和投影数据
- [ ] 所有表达式测试通过
- [ ] 性能满足要求

#### 🚨 风险控制
- **风险**：表达式解析复杂度高
- **控制**：参考现有AST设计，复用组件
- **回滚计划**：保留简单实现，逐步增强

---

### Day 10-12: 查询解析器重构

#### 🎯 目标
重构查询解析器，实现AST到Query的完整转换

#### 📋 检查清单

**Day 10: 解析器架构设计**
- [ ] 分析当前解析器问题
  ```rust
  // 当前状态：返回错误
  Err(QueryError::ParseError(
      "Query parsing needs to be reimplemented for new AST structure".to_string(),
  ))
  ```
- [ ] 设计新的解析器架构
- [ ] 定义转换接口
- [ ] 实现基础转换逻辑
- [ ] 添加错误处理机制

**Day 11: 语句转换实现**
- [ ] 实现MATCH语句转换
- [ ] 实现CREATE语句转换
- [ ] 实现DELETE语句转换
- [ ] 实现UPDATE语句转换
- [ ] 实现GO语句转换
- [ ] 添加测试用例

**Day 12: 完善和优化**
- [ ] 实现参数化查询支持
- [ ] 添加查询缓存机制
- [ ] 优化解析性能
- [ ] 完善错误信息
- [ ] 添加集成测试
- [ ] 验证端到端功能

#### ✅ 验收标准
- [ ] 所有查询类型可以正确解析
- [ ] AST到Query转换完整无误
- [ ] 错误信息清晰有用
- [ ] 解析性能满足要求
- [ ] 所有测试通过

#### 🚨 风险控制
- **风险**：重构可能破坏现有功能
- **控制**：保持接口兼容，渐进式重构
- **回滚计划**：保留旧实现，并行运行验证

---

## 📅 第二阶段：架构优化（第3-4周）

### Day 13-17: 错误处理统一

#### 🎯 目标
建立一致的错误处理机制，提高调试体验

#### 📋 检查清单

**Day 13-14: 错误类型设计**
- [ ] 分析现有错误处理方式
- [ ] 设计统一错误类型
  ```rust
  // 新增：src/query/error.rs
  #[derive(Debug, thiserror::Error)]
  pub enum QueryError {
      // 定义所有查询相关错误
  }
  ```
- [ ] 实现错误转换trait
- [ ] 添加错误上下文信息
- [ ] 设计错误链机制

**Day 15-16: 模块错误处理更新**
- [ ] 更新Parser模块错误处理
- [ ] 更新Validator模块错误处理
- [ ] 更新Planner模块错误处理
- [ ] 更新Optimizer模块错误处理
- [ ] 更新Executor模块错误处理

**Day 17: 错误处理验证**
- [ ] 添加错误处理测试
- [ ] 验证错误信息质量
- [ ] 测试错误传播机制
- [ ] 更新文档和示例

#### ✅ 验收标准
- [ ] 所有模块使用统一错误类型
- [ ] 错误信息清晰有用
- [ ] 错误传播机制正确
- [ ] 错误处理测试覆盖完整

---

### Day 18-24: 模块解耦

#### 🎯 目标
降低模块间耦合度，提高可维护性

#### 📋 检查清单

**Day 18-19: 依赖注入设计**
- [ ] 分析当前模块依赖关系
- [ ] 设计服务接口
  ```rust
  // 新增：src/query/services/mod.rs
  pub trait ParserService {
      fn parse(&self, query: &str) -> Result<AST, QueryError>;
  }
  ```
- [ ] 实现服务容器
- [ ] 设计配置驱动的模块加载

**Day 20-21: 事件驱动架构**
- [ ] 设计事件系统
  ```rust
  pub trait EventBus {
      fn publish(&self, event: QueryEvent);
      fn subscribe(&self, event_type: EventType, handler: Box<dyn EventHandler>);
  }
  ```
- [ ] 实现事件总线
- [ ] 定义查询事件类型
- [ ] 实现事件处理器

**Day 22-24: 模块边界重构**
- [ ] 重构Parser模块边界
- [ ] 重构Planner模块边界
- [ ] 重构Optimizer模块边界
- [ ] 重构Executor模块边界
- [ ] 验证模块独立性
- [ ] 添加集成测试

#### ✅ 验收标准
- [ ] 模块可以独立编译
- [ ] 单元测试不需要依赖其他模块
- [ ] 新功能可以通过插件方式添加
- [ ] 模块间通信通过事件进行

---

## 📅 第三阶段：质量提升（第5-6周）

### Day 25-29: 代码质量提升

#### 🎯 目标
统一代码风格，提高可读性和可维护性

#### 📋 检查清单

**Day 25-26: 代码风格统一**
- [ ] 定义代码规范
- [ ] 统一命名约定
- [ ] 格式化所有代码
  ```bash
  cargo fmt
  ```
- [ ] 运行linter检查
  ```bash
  cargo clippy -- -D warnings
  ```

**Day 27-28: 代码注释完善**
- [ ] 为所有公共接口添加文档注释
- [ ] 为复杂算法添加实现说明
- [ ] 添加使用示例
- [ ] 生成文档并验证

**Day 29: 代码结构优化**
- [ ] 检查函数长度和复杂度
- [ ] 提取公共逻辑
- [ ] 优化模块结构
- [ ] 验证代码质量指标

#### ✅ 验收标准
- [ ] 代码风格统一
- [ ] 所有公共接口有文档
- [ ] 函数复杂度控制在合理范围
- [ ] 代码质量指标达标

---

### Day 30-36: 测试完善

#### 🎯 目标
提高测试覆盖率，增强代码可靠性

#### 📋 检查清单

**Day 30-32: 单元测试完善**
- [ ] 分析当前测试覆盖率
  ```bash
  cargo tarpaulin --out Html
  ```
- [ ] 补充缺失的单元测试
- [ ] 提高测试覆盖率到90%以上
- [ ] 优化测试性能

**Day 33-34: 集成测试添加**
- [ ] 设计端到端测试场景
- [ ] 实现查询流程集成测试
- [ ] 添加复杂查询测试
- [ ] 实现性能基准测试

**Day 35-36: 测试基础设施**
- [ ] 设置持续集成
- [ ] 添加测试数据生成工具
- [ ] 实现测试环境管理
- [ ] 验证所有测试通过

#### ✅ 验收标准
- [ ] 测试覆盖率达到90%以上
- [ ] 所有公共接口有测试
- [ ] 性能测试通过基准
- [ ] 持续集成正常运行

---

## 🎯 总体验收标准

### 功能验收
- [ ] 基本GO查询可以执行并返回正确结果
- [ ] LOOKUP查询可以正确查找和过滤数据
- [ ] PATH查询可以找到正确的路径
- [ ] WHERE条件可以正确过滤数据
- [ ] YIELD子句可以正确聚合和投影数据
- [ ] 所有查询类型都有完整的错误处理

### 质量验收
- [ ] 代码覆盖率达到90%以上
- [ ] 编译警告数少于10个
- [ ] 代码重复率低于5%
- [ ] 平均函数复杂度低于8
- [ ] 查询响应时间小于100ms

### 架构验收
- [ ] 模块可以独立编译和测试
- [ ] 新功能可以通过插件方式添加
- [ ] 错误处理机制统一且完善
- [ ] 代码风格统一且文档完整

## 🔄 持续改进流程

### 日常检查
```bash
# 每日运行
cargo fmt                    # 格式化代码
cargo clippy                 # 代码质量检查
cargo test                   # 运行测试
cargo tarpaulin              # 检查覆盖率
```

### 周期性评估
- **每周**：代码质量指标评估
- **每两周**：架构评审会议
- **每月**：性能基准测试
- **每季度**：技术债务评估

### 文档维护
- 及时更新设计文档
- 记录重要决策和变更
- 维护API文档
- 更新开发指南

## 🚨 应急预案

### 进度延迟应对
1. **评估延迟原因**：识别瓶颈环节
2. **调整优先级**：确保核心功能按时完成
3. **增加资源**：必要时增加开发人员
4. **简化范围**：暂时搁置非关键功能

### 质量问题应对
1. **立即停止**：发现严重质量问题时暂停开发
2. **问题分析**：定位根本原因
3. **制定修复计划**：系统性解决问题
4. **加强审查**：提高代码审查标准

### 技术风险应对
1. **技术预研**：提前验证关键技术方案
2. **备选方案**：准备多种实现方案
3. **专家咨询**：必要时寻求外部技术支持
4. **知识分享**：确保团队技术能力同步

通过这个详细的实施路线图，团队可以系统性地改进 `src/query` 目录，确保每个步骤都有明确的目标、检查清单和验收标准，从而保证改进计划的成功实施。