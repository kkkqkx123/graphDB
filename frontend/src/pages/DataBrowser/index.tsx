import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const DataBrowser: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Data Browser</Title>
      <p>Data browser - Coming soon...</p>
    </Card>
  );
};

export default DataBrowser;
