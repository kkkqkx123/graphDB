import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const Console: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Console</Title>
      <p>Query console - Coming soon...</p>
    </Card>
  );
};

export default Console;
