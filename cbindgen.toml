# cbindgen 配置文件
# 用于生成 C API 头文件

# 语言设置
language = "C"

# 命名空间
namespace = "graphdb"

# 包含的头文件
sys_includes = ["stdbool.h", "stdint.h", "stdlib.h", "stddef.h"]

# 头文件保护宏名称
include_guard = "GRAPHDB_H"

# 是否包含版本信息
include_version = true
